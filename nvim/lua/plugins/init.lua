-- lazy.nvim plugin specs.
--
-- Add plugins by dropping files in this directory (lua/plugins/), each returning
-- a spec or list of specs -- e.g. lua/plugins/lsp.lua, lua/plugins/git.lua.
-- This file itself stays empty; it only exists so the import has an entry point.
-- What's installed and how to drive it: ../../README.md.
return {}
